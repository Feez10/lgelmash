{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Joueur list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Pseudo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('joueurs_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.pseudo }}</td>
                <td>{{ entity.can_be_voted }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('joueurs_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('joueurs_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('joueurs_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
