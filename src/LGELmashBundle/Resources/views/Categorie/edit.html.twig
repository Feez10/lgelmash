{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Modifier la catégorie</h1>

    {{ form(edit_form) }}
    {{ form(delete_form) }}
    <a href="{{ path('categories') }}">Retour à la liste</a>


</ul>
{% endblock %}
